<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Madsense.XamarinForms.Sample.Views.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:Madsense.XamarinForms.Sample.Views;assembly=Madsense.XamarinForms.Sample">
    <ContentPage.Resources>
        <ResourceDictionary>
            <views:ViewA x:Key="SharedViewA" />
            <views:ViewB x:Key="SharedViewB" />

            <ControlTemplate x:Key="FirstTemplate">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>

                    <ContentView Grid.Column="0" Content="{StaticResource SharedViewA}" />
                    <ContentView Grid.Column="1" Content="{StaticResource SharedViewB}" />
                </Grid>
            </ControlTemplate>
            <ControlTemplate x:Key="SecondTemplate">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*" />
                        <RowDefinition Height="1*" />
                    </Grid.RowDefinitions>

                    <ContentView Grid.Row="0" Content="{StaticResource SharedViewA}" />
                    <ContentView Grid.Row="1" Content="{StaticResource SharedViewB}" />
                </Grid>
            </ControlTemplate>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Command="{Binding SwitchTemplateCommand}" Text="Switch Template" />

        <ContentView Grid.Row="1" ControlTemplate="{StaticResource FirstTemplate}">
            <ContentView.Triggers>
                <DataTrigger
                    Binding="{Binding IsFirstTemplate}"
                    TargetType="ContentView"
                    Value="False">
                    <Setter Property="ControlTemplate" Value="{StaticResource SecondTemplate}" />
                </DataTrigger>
            </ContentView.Triggers>
        </ContentView>
    </Grid>
</ContentPage>
