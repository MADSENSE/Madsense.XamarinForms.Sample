<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Madsense.XamarinForms.Sample.Views.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:Madsense.XamarinForms.Sample.Converters;assembly=Madsense.XamarinForms.Sample"
    xmlns:mvvm="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    xmlns:views="clr-namespace:Madsense.XamarinForms.Sample.Views;assembly=Madsense.XamarinForms.Sample"
    x:Name="Root">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:IsSmallerThanParameterConverter x:Key="IsSmallerThanParameterConverter" />
            <x:Double x:Key="MinimumScreenWidth">768</x:Double>

            <views:PartialViewA x:Key="SharedPartialViewA" mvvm:ViewModelLocator.AutowirePartialView="{x:Reference Root}" />
            <views:PartialViewB x:Key="SharedPartialViewB" mvvm:ViewModelLocator.AutowirePartialView="{x:Reference Root}" />

            <ControlTemplate x:Key="FirstTemplate">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>
                    <ContentView Grid.Column="0" Content="{StaticResource SharedPartialViewA}" />
                    <ContentView Grid.Column="1" Content="{StaticResource SharedPartialViewB}" />
                </Grid>
            </ControlTemplate>
            <!--<ControlTemplate x:Key="SecondTemplate">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>
                    <ContentView Grid.Column="1" Content="{StaticResource SharedPartialViewA}" />
                    <ContentView Grid.Column="0" Content="{StaticResource SharedPartialViewB}" />
                </Grid>
            </ControlTemplate>-->
            <ControlTemplate x:Key="SecondTemplate">
                <CarouselView>
                    <CarouselView.ItemsSource>
                        <x:Array Type="{x:Type ContentView}">
                            <ContentView Content="{DynamicResource SharedPartialViewA}" />
                            <ContentView Content="{DynamicResource SharedPartialViewB}" />
                        </x:Array>
                    </CarouselView.ItemsSource>
                    <CarouselView.ItemTemplate>
                        <DataTemplate>
                            <ContentView Content="{Binding .}" />
                        </DataTemplate>
                    </CarouselView.ItemTemplate>
                </CarouselView>
            </ControlTemplate>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Command="{Binding SwitchTemplateCommand}" Text="Switch Template" />

        <ContentView Grid.Row="1" ControlTemplate="{StaticResource FirstTemplate}">
            <!--<ContentView.Triggers>
                <MultiTrigger TargetType="ContentView">
                    <MultiTrigger.Conditions>
                        <BindingCondition Binding="{Binding IsFirstTemplate}" Value="False" />
                    </MultiTrigger.Conditions>
                    <Setter Property="ControlTemplate" Value="{StaticResource SecondTemplate}" />
                </MultiTrigger>
            </ContentView.Triggers>-->
            <ContentView.Triggers>
                <MultiTrigger TargetType="ContentView">
                    <MultiTrigger.Conditions>
                        <BindingCondition Binding="{Binding Source={x:Reference Root}, Path=Width, Converter={StaticResource IsSmallerThanParameterConverter}, ConverterParameter={StaticResource MinimumScreenWidth}}" Value="True" />
                        <BindingCondition Binding="{Binding Source={x:Reference Root}, Path=Width, Converter={StaticResource IsSmallerThanParameterConverter}, ConverterParameter=0}" Value="False" />
                    </MultiTrigger.Conditions>
                    <Setter Property="ControlTemplate" Value="{StaticResource SecondTemplate}" />
                </MultiTrigger>
            </ContentView.Triggers>
        </ContentView>
    </Grid>
</ContentPage>